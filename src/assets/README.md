# assets

Static app resources:
- images: chapter illustrations.
- sounds: effects and story event audio.

Important:
- File names must match image fields in story JSON.
- Sounds must be registered in src/utils/soundPlayer.js.

Target:
- Story-based organization (subfolders per story).
- Automatic validation for missing assets.