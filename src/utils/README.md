# utils

Shared cross-cutting utilities.

Current file:
- soundPlayer.js
  - Centralizes audio playback with expo-av.

Current state:
- Base audio playback infrastructure is available.

Target:
- More robust audio API (events, fallback, preload).
- Safer error handling to improve UX.
